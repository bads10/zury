FROM python:3.11-slim

WORKDIR /app

COPY api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY api/ ./api/

RUN mkdir -p /app/media /app/uploads

CMD ["celery", "-A", "api.workers.tasks", "worker", "--loglevel=info", "--concurrency=2"]
